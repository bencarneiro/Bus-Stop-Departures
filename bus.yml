name: bus

channels:
  - conda-forge

dependencies:
  - python=3.7
  - django
  - jupyterlab
  - geopandas
  - osmnx
  - pip
  - pip:
      - django-cors-headers
      - djangorestframework
      - gtfs-realtime-bindings
