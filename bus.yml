name: bus

channels:
  - conda-forge

dependencies:
  - django
  - jupyterlab
  - geopandas
  - osmnx
  - pip
  - pip:
      - django-cors-headers
      - djangorestframework
      - gtfs-realtime-bindings
